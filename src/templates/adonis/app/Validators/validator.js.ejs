'use strict'

class <%= props.className%> {
  get validateAll () {
    return true
  }

  get rules () {
    return {<% if(!props.create){for(var i=0; i < props.fields.length; i++) { %>
      <%= props.fields[i].field %>: 'required',<% }} %>
    }
  }

  get messages () {
    return {<% if(!props.create){for(var i=0; i < props.fields.length; i++) { %>
      '<%= props.fields[i].field %>.required': 'Você deve informar o campo <%= props.fields[i].field %>',<% }} %>
    }
  }
}

module.exports = <%= props.className%>
