import { createReducer, createActions } from 'reduxsauce';
import Immutable from 'seamless-immutable';

// Action Types & Creators

const { Types, Creators } = createActions({
  example: ['data'],
});

export const <%= props.capital %>Types = Types;
export const <%= props.capital %>Actions = Creators;

// Initial State

export const INITIAL_STATE = Immutable({
  data: [],
});

// Reducer Functions

const exampleImplementation = (state, { data }) => ({ ...state, data });

// Reducer

export const <%= props.capital %>Reducer = createReducer(INITIAL_STATE, {
  [Types.EXAMPLE]: exampleImplementation,
});
