/**
 * @format
 */

import 'react-native';
import React from 'react';
import <%= props.name%> from './<%= props.name%>';

import renderer from 'react-test-renderer';

it('renders correctly', () => {
  renderer.create(<<%= props.name%> />);
});
