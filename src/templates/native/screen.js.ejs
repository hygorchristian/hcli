import React from 'react';

import { Container } from './styles';

function <%= props.filename%>() {
  return <Container />;
}

<%= props.filename%>.propTypes = {
  // props here
};

export default <%= props.filename%>;
