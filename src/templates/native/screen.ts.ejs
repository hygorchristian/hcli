import React from 'react'

import { Container } from './styles'

interface <%= props.filename%>Props {}

const  <%= props.filename%> = (props: <%= props.filename%>Props) => {
  return <Container />
}

export default <%= props.filename%>
