import React from "react";

import { Container } from "./styles";

interface <%= props.filename%>Props {}

export const <%= props.camelname%>Assets = [];

function <%= props.filename%>(props: <%= props.filename%>Props) {
  return <Container {...props} />;
}

export default <%= props.filename%>;
