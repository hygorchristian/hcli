// @ts-nocheck
// eslint-disable-next-line import/no-extraneous-dependencies
import { Story, Meta } from '@storybook/react/types-6-0';

import <%= props.filename%>, { <%= props.filename%>Props } from './<%= props.filename%>';

export default {
  title: 'Example/<%= props.filename%>',
  component: <%= props.filename%>,
  argTypes: {},
} as Meta;

const Template: Story<<%= props.filename%>Props> = args => <<%= props.filename%> {...args} />;
